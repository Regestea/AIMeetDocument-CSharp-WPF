<UserControl x:Class="AIMeetDocument.AiProccess"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="600">
    <Border Background="#212735" CornerRadius="20" Padding="32" Margin="16,0,0,0" Height="400" Width="600"
            Effect="{DynamicResource CardDropShadow}">
        <Border.BorderBrush>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FCE49C" Offset="0.0"/>
                <GradientStop Color="#F29D3A" Offset="1.0"/>
            </LinearGradientBrush>
        </Border.BorderBrush>
        <Border.BorderThickness>2</Border.BorderThickness>
        <StackPanel>
            <!-- File Name Display -->
            <TextBlock x:Name="FileNameText" Text="FileName.txt" FontSize="16" FontWeight="Bold" Foreground="#FCE49C" Margin="0,0,0,12"/>

            <!-- Language Selection -->
            <TextBlock Text="Language" Foreground="#fce6ac" FontWeight="SemiBold" FontSize="13"/>
            <ComboBox x:Name="LanguageCombo" SelectedIndex="0" Margin="0,4,0,12" Height="28" MinWidth="120">
                <ComboBox.Style>
                    <Style TargetType="ComboBox">
                        <Setter Property="BorderBrush" Value="#efa449"/>
                        <Setter Property="BorderThickness" Value="2"/>
                        <Setter Property="Background" Value="#fadc93"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBox">
                                    <Border x:Name="Bd" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" CornerRadius="5">
                                        <Grid>
                                            <ToggleButton x:Name="ToggleButton" Grid.Column="2" Focusable="false"
                                                IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                ClickMode="Press"
                                                Background="Transparent"
                                                BorderThickness="0">
                                                <Path x:Name="Arrow" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M 0 0 L 4 4 L 8 0 Z" Fill="#FFD600" Width="8" Height="4"/>
                                            </ToggleButton>
                                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="6,0,24,0" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                                <Grid MinWidth="{TemplateBinding ActualWidth}" MaxHeight="200" Background="#212735">
                                                    <Border x:Name="DropDownBorder" BorderBrush="#FFD600" BorderThickness="2" CornerRadius="5" Background="#212735">
                                                        <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                                        </ScrollViewer>
                                                    </Border>
                                                </Grid>
                                            </Popup>
                                        </Grid>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ComboBox.Style>
                <ComboBoxItem Content="English" Tag="en"/>
                <ComboBoxItem Content="Persian" Tag="fa"/>
                <ComboBoxItem Content="Arabic" Tag="ar"/>
                <ComboBoxItem Content="Chinese" Tag="zh"/>
                <ComboBoxItem Content="French" Tag="fr"/>
                <ComboBoxItem Content="German" Tag="de"/>
                <ComboBoxItem Content="Hindi" Tag="hi"/>
                <ComboBoxItem Content="Italian" Tag="it"/>
                <ComboBoxItem Content="Japanese" Tag="ja"/>
                <ComboBoxItem Content="Korean" Tag="ko"/>
                <ComboBoxItem Content="Russian" Tag="ru"/>
                <ComboBoxItem Content="Spanish" Tag="es"/>
                <ComboBoxItem Content="Turkish" Tag="tr"/>
            </ComboBox>

            <!-- File Type Selection -->
            <TextBlock Text="File Type" Foreground="#fce6ac" FontWeight="SemiBold" FontSize="13"/>
            <ComboBox x:Name="FileTypeCombo" SelectedIndex="0" Margin="0,4,0,12" Height="28" MinWidth="120">
                <ComboBox.Style>
                    <Style TargetType="ComboBox">
                        <Setter Property="BorderBrush" Value="#efa449"/>
                        <Setter Property="BorderThickness" Value="2"/>
                        <Setter Property="Background" Value="#fadc93"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBox">
                                    <Border x:Name="Bd" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" CornerRadius="5">
                                        <Grid>
                                            <ToggleButton x:Name="ToggleButton" Grid.Column="2" Focusable="false"
                                                IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                ClickMode="Press"
                                                Background="Transparent"
                                                BorderThickness="0">
                                                <Path x:Name="Arrow" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M 0 0 L 4 4 L 8 0 Z" Fill="#FFD600" Width="8" Height="4"/>
                                            </ToggleButton>
                                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="6,0,24,0" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                                <Grid MinWidth="{TemplateBinding ActualWidth}" MaxHeight="200" Background="#212735">
                                                    <Border x:Name="DropDownBorder" BorderBrush="#FFD600" BorderThickness="2" CornerRadius="5" Background="#212735">
                                                        <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                                        </ScrollViewer>
                                                    </Border>
                                                </Grid>
                                            </Popup>
                                        </Grid>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ComboBox.Style>
                <ComboBoxItem Content="Word"/>
                <ComboBoxItem Content="PDF"/>
                <ComboBoxItem Content="MD"/>
            </ComboBox>

            <!-- Save Location -->
            <TextBlock Text="Save Location" Foreground="#fce6ac" FontWeight="SemiBold" FontSize="13"/>
            <DockPanel Margin="0,4,0,12" LastChildFill="False">
                <TextBox x:Name="LocationTextBox" Width="385" Height="28" VerticalAlignment="Center" Margin="0,0,8,0" IsReadOnly="True">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="BorderBrush" Value="#efa449"/>
                            <Setter Property="BorderThickness" Value="2"/>
                            <Setter Property="Background" Value="#fce6ac"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TextBox">
                                        <Border x:Name="Bd" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" CornerRadius="5">
                                            <ScrollViewer x:Name="PART_ContentHost"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                <Button x:Name="BrowseButton" Content="Save Location" Width="120" Height="28" Style="{DynamicResource ModernButton}" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="5">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </DockPanel>

            <!-- Start Button Centered -->
            <StackPanel x:Name="ActionPanel" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,0">
                <Button x:Name="StartButton" Content="Start" Height="36" Width="120" Style="{DynamicResource ModernButton}" Cursor="Hand"/>
            </StackPanel>

            <!-- Modern Loading & Cancel Centered -->
            <StackPanel x:Name="LoadingPanel" Orientation="Horizontal" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,24,0,0" Height="48">
                <Viewbox Width="48" Height="48" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,0,16,0">
                    <Grid Width="40" Height="40">
                        <Ellipse Width="40" Height="40" StrokeThickness="5" Stroke="#FFD600" Opacity="0.15"/>
                        <Ellipse Width="40" Height="40" StrokeThickness="5" StrokeDashArray="0,2 10,2" Stroke="#F29D3A">
                            <Ellipse.RenderTransform>
                                <RotateTransform x:Name="SpinnerRotate" Angle="0" CenterX="20" CenterY="20"/>
                            </Ellipse.RenderTransform>
                            <Ellipse.Triggers>
                                <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                                    <BeginStoryboard>
                                        <Storyboard RepeatBehavior="Forever">
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)" From="0" To="360" Duration="0:0:0.7"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Ellipse.Triggers>
                        </Ellipse>
                    </Grid>
                </Viewbox>
                <Button x:Name="CancelButton" Content="Cancel" Height="40" Width="100" Style="{DynamicResource ModernButton}"
                        HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,0" Cursor="Hand"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
